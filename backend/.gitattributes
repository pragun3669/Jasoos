# Set default behavior for all files to let Git decide.
# This handles binary files correctly.
* text=auto

# Normalize text files to LF line endings in the repository.
# This is the standard for cross-platform projects.
*.java      text eol=lf
*.xml       text eol=lf
*.properties text eol=lf
*.sql       text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
*.md        text eol=lf
.gitignore  text eol=lf
.gitattributes text eol=lf

# Shell scripts must have LF endings to be executable on Linux/macOS/WSL.
mvnw        text eol=lf
*.sh        text eol=lf

# Windows batch files must have CRLF endings to be executable on Windows.
mvnw.cmd    text eol=crlf
*.bat       text eol=crlf
*.cmd       text eol=crlf